@using (Html.BeginForm(null, null, FormMethod.Post, new { id = "emailQueryForm" }))
{
    @Html.ValidationSummary();
    <input type="text" name="query" class="sqlQuery" style="display:none;" value="@ViewBag.Query" />
            <div>
                @Html.Label("E-mail:")
                <div>
                    <input type="email" name="email" required="required"/>
                </div>
            </div>
            <input type="submit" />
}

<script>
        $('.ui-dialog-buttonset button').addClass("ui-button ui-corner-all ui-widget button-modal");
        $('.ui-dialog-buttonset button').css('width', 70);
        $('.ui-dialog-buttonset button').css('height', 30);
</script>